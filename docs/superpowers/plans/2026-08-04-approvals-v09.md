# 审批中心 V0.9 实施计划

1. 以失败契约测试约束 `approvals`、`approval_steps`、`approval_actions`、索引和 RLS，再新增 migration。
2. 以失败测试约束审批类型、Mock 数据、统计、筛选与详情选择器，再实现数据层。
3. 以失败页面测试约束列表、详情和同意/拒绝确认交互，再实现组件与路由。
4. 启用 Sidebar 审批入口，执行单元、类型、规范、构建和浏览器测试。
5. 生成桌面/移动截图，与既有审批设计稿完成同视口视觉检查。
