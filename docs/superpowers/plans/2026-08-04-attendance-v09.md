# 考勤管理 V0.9 实施计划

1. 先写 migration 契约测试，确认 `attendance` 表、唯一索引、状态约束、RLS 与权限边界；观察测试失败后补 migration。
2. 先写考勤类型、统计和筛选测试；观察失败后实现类型、Mock 数据与纯选择器。
3. 先写页面组件测试；观察失败后实现统计卡、筛选栏、桌面表格、移动卡片、月度趋势和异常提醒。
4. 新增 `/attendance` 路由并只启用对应 Sidebar 项，不改写已有模块。
5. 新增浏览器主流程测试，验证筛选、响应式、控制台错误和截图。
6. 执行单元测试、类型检查、代码规范、生产构建和浏览器测试。
7. 将桌面参考图与实现截图放入同一视觉检查输入，修正差异并更新 `design-qa.md` 为 `passed`。
