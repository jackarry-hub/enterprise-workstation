# Activities

活动推进是项目协同的执行视图，不建立独立业务模型。

- `activity-types.ts`：复用 Project、Milestone、ProjectTask 的页面聚合契约。
- `activity-mock-data.ts`：基于已有项目、成员和任务 Mock 组合活动视图。
- `activities-page.tsx`：`/activities` 的列表与详情状态组合。
- `components/`：活动统计、活动列表和活动详情组件。

活动目标、阶段、指标与推进任务。
