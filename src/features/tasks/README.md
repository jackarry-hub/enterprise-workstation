# Tasks

工作中心承接“项目任务 → 日常执行 → 日报反馈 → 项目动态”的员工每日工作流程。

- `workspace-types.ts`：工作中心页面聚合数据契约。
- `workspace-mock-data.ts`：Demo V1.0 的任务、待办、日报和动态数据。
- `workspace-data.ts`：为后续 Supabase 接入保留的同源数据装配边界。
- `workspace-page.tsx`：`/workspace` 页面组合入口。
- `components/`：今日概览、任务列表、待办、日报和最近动态组件。

任务、子任务、状态、负责人和视图切换。
