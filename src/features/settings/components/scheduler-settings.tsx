import { Clock3, WandSparkles } from "lucide-react";
import { Input } from "@/components/ui/input";
import type { SettingsState } from "@/features/settings/settings-types";

export function SchedulerSettings({ value, onChange, disabled }: { value: SettingsState["scheduler"]; onChange: (value: SettingsState["scheduler"]) => void; disabled: boolean }) {
  const patch = (next: Partial<SettingsState["scheduler"]>) => onChange({ ...value, ...next });
  return <section aria-labelledby="scheduler-settings-title"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-2xl bg-violet-100 text-violet-700"><WandSparkles className="size-5" /></span><div><h2 id="scheduler-settings-title" className="text-xl font-semibold">调度参数</h2><p className="text-sm text-muted-foreground">供智能排期使用的企业工作时段与默认边界。</p></div></div><div className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><label className="grid gap-2 text-sm font-medium">工作开始<Input disabled={disabled} aria-label="工作开始" type="time" value={value.workdayStart} onChange={(event) => patch({ workdayStart: event.target.value })} /></label><label className="grid gap-2 text-sm font-medium">工作结束<Input disabled={disabled} aria-label="工作结束" type="time" value={value.workdayEnd} onChange={(event) => patch({ workdayEnd: event.target.value })} /></label><label className="grid gap-2 text-sm font-medium">默认计划天数<Input disabled={disabled} aria-label="默认计划天数" type="number" min={1} max={90} value={value.defaultPlanDays} onChange={(event) => patch({ defaultPlanDays: Number(event.target.value) })} /></label><label className="grid gap-2 text-sm font-medium">每日工时上限<div className="relative"><Clock3 className="pointer-events-none absolute top-1/2 left-3 size-4 -translate-y-1/2 text-muted-foreground" /><Input disabled={disabled} aria-label="每日工时上限" type="number" min={1} max={16} value={value.maxDailyHours} onChange={(event) => patch({ maxDailyHours: Number(event.target.value) })} className="pl-9" /></div></label></div></section>;
}
