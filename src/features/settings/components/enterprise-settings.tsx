import Image from "next/image";
import { Building2, CalendarDays, Clock3, Languages } from "lucide-react";

import { Input } from "@/components/ui/input";
import { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from "@/components/ui/select";
import type { SettingsState } from "@/features/settings/settings-types";

export function EnterpriseSettings({ value, onChange, disabled }: { value: SettingsState["organization"]; onChange: (value: SettingsState["organization"]) => void; disabled: boolean }) {
  const patch = (next: Partial<SettingsState["organization"]>) => onChange({ ...value, ...next });
  return <section aria-labelledby="enterprise-settings-title"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-2xl bg-primary/10 text-primary"><Building2 className="size-5" /></span><div><h2 id="enterprise-settings-title" className="text-xl font-semibold">企业信息</h2><p className="text-sm text-muted-foreground">企业级配置由管理员保存到工作区数据库。</p></div></div>{disabled ? <p className="mt-4 rounded-xl bg-muted px-3 py-2 text-xs text-muted-foreground">当前为只读视图；需要 settings.manage 权限才能修改。</p> : null}<div className="mt-5 grid gap-4 lg:grid-cols-2 2xl:grid-cols-4"><label className="grid gap-2 text-sm font-medium">企业名称<Input aria-label="企业名称" disabled={disabled} value={value.name} onChange={(event) => patch({ name: event.target.value })} /></label><label className="grid gap-2 text-sm font-medium">企业简称<Input aria-label="企业简称" disabled={disabled} value={value.shortName} onChange={(event) => patch({ shortName: event.target.value })} /></label><label className="grid gap-2 text-sm font-medium">Logo 路径<Input aria-label="Logo 路径" disabled={disabled} value={value.logoUrl} onChange={(event) => patch({ logoUrl: event.target.value })} placeholder="/brand/company-logo.png" /></label><div className="grid place-items-center rounded-2xl border bg-background/60 p-3">{value.logoUrl.startsWith("/") ? <Image src={value.logoUrl} alt={`${value.shortName || value.name}企业 Logo`} width={160} height={56} className="max-h-14 w-auto object-contain" /> : <Building2 className="size-8 text-muted-foreground" />}</div><label className="grid gap-2 text-sm font-medium">所在时区<Select disabled={disabled} value={value.timezone} onValueChange={(timezone) => patch({ timezone })}><SelectTrigger aria-label="所在时区"><Clock3 className="size-4" /><SelectValue /></SelectTrigger><SelectContent><SelectItem value="asia-shanghai">Asia/Shanghai</SelectItem><SelectItem value="asia-singapore">Asia/Singapore</SelectItem></SelectContent></Select></label><label className="grid gap-2 text-sm font-medium">系统语言<Select disabled={disabled} value={value.language} onValueChange={(language) => patch({ language })}><SelectTrigger aria-label="系统语言"><Languages className="size-4" /><SelectValue /></SelectTrigger><SelectContent><SelectItem value="zh-cn">简体中文</SelectItem><SelectItem value="en">English</SelectItem></SelectContent></Select></label><label className="grid gap-2 text-sm font-medium">企业成立日期<div className="relative"><CalendarDays className="pointer-events-none absolute top-1/2 left-3 size-4 -translate-y-1/2 text-muted-foreground" /><Input aria-label="企业成立日期" disabled={disabled} type="date" value={value.foundedDate} onChange={(event) => patch({ foundedDate: event.target.value })} className="pl-9" /></div></label><label className="grid gap-2 text-sm font-medium">工作周开始于<Select disabled={disabled} value={value.workWeekStart} onValueChange={(workWeekStart) => patch({ workWeekStart })}><SelectTrigger aria-label="工作周开始于"><SelectValue /></SelectTrigger><SelectContent><SelectItem value="monday">周一</SelectItem><SelectItem value="sunday">周日</SelectItem></SelectContent></Select></label></div></section>;
}
